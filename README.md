# CliniAI - Backend Service

This is the central clinical backend, responsible for managing healthcare data, processing business logic, and orchestrating interactions with the database and AI services via event listeners.

## Setup
1. Set up a virtual environment: `python -m venv venv`
2. Install requirements: `pip install -r requirements.txt`
3. Configurations exist for event listening and generic clinical data CRUD.
